# Local fallback only. Prefer Soup for shared secrets:
#   pnpm secrets:login
#   pnpm secrets:config
#   pnpm dev:secrets

# Server-only secrets
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
USDA_FDC_API_KEY=
GOOGLE_PLACES_API_KEY=

# Map providers. Prefer a restricted public token if exposed to the browser.
MAPTILER_API_KEY=
MAPBOX_ACCESS_TOKEN=
NEXT_PUBLIC_MAPTILER_API_KEY=
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
